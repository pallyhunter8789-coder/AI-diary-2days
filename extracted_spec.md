AI 시간다이어리 웹앱 — Antigravity 프롬프트 플레이북 (순서대로)
목표: 명세서(AI시간다이어리_설문웹_빌드명세서_v1.md)를 바탕으로, 테헤란씨씨가 직접 운영할 모바일 우선 설문 웹앱을 Antigravity로 빌드. 전제: 접속의 90% 이상이 모바일 → 모든 화면은 모바일 우선, 터치 최적화. 데스크톱은 가운데 정렬(max-width 480px). 투트랙: 패널사(멤브레인·패널나우) 응답자와 자체 모집 응답자가 같은 앱·같은 DB로 들어옴(토큰의 panel_source로 구분). 앱 빌드는 하나로 동일.


시작 전 준비 (1회)
Antigravity에서 새 워크스페이스(빈 폴더) 생성.
그 폴더에 두 파일을 복사해 넣기:
AI시간다이어리_설문웹_빌드명세서_v1.md
AI시간다이어리_프로토타입_다이어리화면.html
아래 프롬프트를 ① → ⑩ 순서로 하나씩 붙여넣기. 각 단계가 끝나면 "확인" 항목을 점검하고 다음으로.
막히면: "방금 만든 화면을 모바일 폭(390px)에서 스크린샷 찍어서 보여줘"라고 시켜 눈으로 확인.


① 프로젝트 브리핑 + 스캐폴드
이 워크스페이스에 있는 'AI시간다이어리_설문웹_빌드명세서_v1.md'를 먼저 읽어줘. 이 명세서대로 설문 웹앱을 만들 거야.

지금은 1단계: 프로젝트 스캐폴드만.

- Next.js 14(App Router) + TypeScript + Tailwind CSS로 새 프로젝트 생성

- @supabase/supabase-js 설치

- 폰트 Pretendard(웹폰트), 디자인 토큰: navy #203864, cyan #04BEE7 를 Tailwind theme와 globals.css에 등록

- 가장 중요한 원칙: 접속의 90%가 모바일이다. 모바일 우선으로 만들고, 앱은 모바일에서 화면 전체를 꽉 채운다(데스크톱에서는 max-width 480px 가운데 정렬). viewport meta, 100dvh, safe-area-inset 반영.

- 루트 레이아웃과 빈 홈 페이지만 만들고, 'npm run dev'로 실행되는 상태까지만.

- .env.local에 NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY 자리만 만들어줘(값은 비워두고).

아직 화면 기능은 만들지 마. 스캐폴드와 디자인 토큰까지만.

확인: dev 서버 실행됨 / 모바일 폭에서 여백 없이 꽉 차는 빈 화면 / navy·cyan 토큰 적용.


② 데이터베이스 & 서버 연결
2단계: 데이터베이스.

- 명세서 '§2 데이터 모델'의 SQL 전체를 supabase/migrations/0001_init.sql 파일로 만들어줘(그대로).

- 명세서 '§3 참조 데이터'의 시드(직종 할당, 성별×연령 quota_cells, AI 도구 카탈로그, 활동/장소/목적)를 supabase/seed.sql로 만들어줘.

- 서버 전용 Supabase 클라이언트(lib/supabaseServer.ts)를 service_role 키로 만들고, 클라이언트에는 절대 노출하지 마.

- 응답자는 DB에 직접 접근하지 않는다. 모든 읽기/쓰기는 app/api/* 라우트 핸들러를 경유하도록 구조를 잡아줘.

- 타입(types/db.ts)도 스키마에 맞춰 정의해줘.

마이그레이션/시드 실행 방법을 README에 적어줘.

확인: 0001_init.sql·seed.sql 생성 / supabaseServer는 서버에서만 import / API 경유 구조.


③ 토큰 진입 + 상태 분기(라우팅)
3단계: 진입과 상태 머신.

- 명세서 '§4 화면 흐름'과 '§5.1 진입'대로 구현.

- '/?t=토큰' 으로 들어오면 GET /api/respondent?t= 로 검증하고 respondents.status에 따라 분기:

  invited→/screen/1, screened_in→/diary/1, day1_done→/diary/2 안내, completed→/complete

- 토큰 없음/만료면 안내 화면(에러는 사과체 말고 '무엇을 어떻게' 안내).

- 재진입 시 마지막 위치로 복귀(이어쓰기). events 테이블에 'open' 기록.

- 모바일 풀스크린 셸 레이아웃(상단 고정 헤더 영역 + 본문 스크롤 + 하단 고정 액션 영역) 컴포넌트를 만들어 이후 화면이 재사용하게 해줘.

테스트용으로 토큰 1개를 시드에 넣어줘(panel_source='self').

확인: 테스트 토큰으로 진입 시 상태별 이동 / 셸 레이아웃 재사용 가능.


④ 스크리닝(2단계) + 동의 + 온보딩
4단계: 스크리닝.

- 명세서 '§5.2~5.5'대로 /screen/1, /screen/2, /consent, /onboarding 구현.

- S1: 연령(만19세 미만 탈락), 고용형태(임금근로자/자영업자/해당없음→탈락), 성별.

- S2: AI 사용빈도(주2회 미만 탈락), AI 도구·사용목적 자유기술(공백/무의미→저품질 탈락), 직종(KSCO 대분류; 농림어업·군인 선택 시 '대상 외' 종료).

- 통과 시 age_band×sex로 quota_cell_sexage, 직종으로 quota_cell_ksco 배정 → v_quota_progress로 셀 마감 검사 → 마감이면 quota_full 종료, 아니면 screened_in.

- 모든 입력은 모바일 한 손 조작 기준: 큰 버튼/칩, 한 화면 한 질문 흐름, 하단 '다음' 고정.

- 저장은 POST /api/screening.

확인: 탈락/마감/통과 분기 정상 / 모바일에서 한 손 입력 편함 / quota 셀 배정됨.


⑤ 다이어리 핵심 화면 ★가장 중요
5단계: 다이어리 입력 화면 /diary/[day] — 이 앱의 핵심.

- 같은 폴더의 'AI시간다이어리_프로토타입_다이어리화면.html'을 열어서 레이아웃·상호작용·색상을 그대로 참고해(이게 정답 디자인이다). 단, 프로토타입의 '폰 목업 프레임'은 빼고 실제 앱은 모바일 화면을 꽉 채운다.

- 명세서 '§5.6'과 '§0 확정 파라미터' 준수: 08:00–24:00, 30분 슬롯 32칸(index 0~31), 세로 타임라인.

- 모바일이 90%이므로 터치에서 드래그가 페이지 스크롤과 싸우지 않게 만든다. 권장 상호작용:

  · 빈 슬롯 '탭' = 30분 블록 추가 → 하단 시트 열림

  · 블록의 아래쪽 핸들을 '드래그'해서 길이 연장/축소(resize)

  · (데스크톱/롱프레스) 빈 칸 세로 드래그로 범위 한번에 칠하기 = 보조 기능

  · 페이지 스크롤은 네이티브 유지. 드래그 중에만 touch-action: none.

- 블록 탭 = 수정. 하단 시트(bottom sheet)에서: 활동 대분류(칩)+세부(직접입력), 장소(칩), AI 사용 토글 → ON이면 AI 유형(칩)→도구명(유형별 추천 + 목록 외 직접입력)→사용목적(칩). 도구는 +추가로 여러 개(entry_ai_tools).

- AI 사용 블록은 cyan 좌측 보더 + ✨ 'AI' 배지로 강조(한눈에 AI 사용 시간대가 보이게).

- 상단: 진행률 바(채운 슬롯/32), '자동 저장됨' 표시. 보조: '직전과 동일' 복제 버튼.

- 자동저장: 블록 추가/수정/삭제 시 800ms 디바운스 후 POST /api/diary/entry. 슬롯 범위 중복 금지.

- 새로고침/재진입 시 작성 내용 복원(GET /api/diary?day=).

- 터치 타깃 최소 44px, 한 손 도달 영역에 주요 버튼.

이 화면이 모바일에서 매끄럽게 동작하는 걸 최우선으로 해줘. 끝나면 390px 폭 스크린샷을 보여줘.

확인(모바일): 탭으로 블록 추가 / 핸들 드래그 resize / AI 토글·도구 직접입력 / ✨ 배지 / 자동저장·복원 / 스크롤과 안 싸움.


⑥ 완료 흐름(2일 연속)
6단계: 완료 흐름.

- 명세서 '§5.7~5.10'대로.

- Day1 완료: 빈 슬롯 큰 공백 확인 → diary_days.status=completed, respondents.status=day1_done, 사후 미니문항(체감부담·소요시간) → '다음 평일에 2일차 안내' 화면 + events 'reminder_sent' 훅(실제 발송은 외부 연계, 지금은 인터페이스만).

- Day2 진입(동일 토큰)→ /diary/2 → 완료.

- /finish: 기본정보(거주지역·직업 중분류·월평균 근로시간·소득분위 1~10) + Day2 사후 미니문항 → '제출' → respondents.status=completed(이때만 유효표본). events 'submit'.

- /complete: 감사 + 사례비 정산 안내.

- 유효표본은 반드시 '2일 연속 완료(completed)'만. day1_done은 유효 아님.

확인: Day1→Day2→제출 시에만 completed / 부분 완료는 유효 아님 / 사후문항 저장.


⑦ 관리자 대시보드 + 내보내기
7단계: 관리자 화면 /admin (간단 비밀번호 보호).

- 명세서 '§5.11, §7, §8'대로.

- 요약 카드: 초대/스크리닝 통과/Day1완료/2일완료/이탈, 2일 완료율(%).

- 셀별 진행률 표 2개(성별×연령, 직종): target 대비 completed/in_progress, 부족 셀 하이라이트.

- 품질 플래그 목록, events 기반 이탈 시점 분포.

- 내보내기 버튼: ① 원시(블록 join) ② long-format(블록을 슬롯 0~31로 전개: respondent_id, day_no, slot, time, activity, location, ai_used, ai_type, ai_tool, purpose) ③ 코드북. CSV/XLSX.

- 관리자 화면은 데스크톱에서 봐도 되니 표 중심으로.

확인: 셀 진행률·완료율 표시 / long-format CSV가 슬롯 단위로 펼쳐져 나옴.


⑧ 품질 룰엔진 + 패러데이터
8단계: 품질 관리.

- 명세서 '§6'대로 quality_flags 생성(자동 탈락 아님, 검수 표시): speeding(과속), straightline(직선), duplicate_pattern(Day1=Day2), attention_fail(숨은 어텐션 체크 1문항), device_dup.

- 어텐션 체크 문항 1개를 스크리닝 또는 사후에 자연스럽게 삽입.

- events 로깅을 전 화면에 심어 이탈·소요시간·재진입을 추적.

- 관리자 화면에 플래그가 뜨도록 연결.

확인: 플래그 생성·노출 / 어텐션 체크 동작 / events 누적.


⑨ 모바일 QA & 마감 점검
9단계: 모바일 품질 점검 패스. 다음을 직접 점검하고 문제를 고쳐줘.

- 360 / 390 / 414px 폭에서 가로 스크롤 없음, 잘림 없음, 하단 고정 버튼이 키보드/홈바와 안 겹침(safe-area).

- 다이어리에서 탭·드래그가 스크롤과 충돌 없음, 바텀시트 열고 닫기 자연스러움.

- 터치 타깃 ≥44px, 포커스 표시, prefers-reduced-motion 존중.

- 느린 네트워크에서 자동저장 실패 시 재시도/표시.

- 새로고침·앱 전환 후 작성 내용 복원 확인.

- 각 핵심 화면(스크리닝, 다이어리 입력, AI 태깅 시트, 완료)을 390px로 스크린샷 찍어서 4컷 보여줘.

확인: 4컷 스크린샷 확보(→ 제안서 §II-4·발표자료에 사용) / 모바일 이슈 해결.


⑩ 배포(Vercel)
10단계: 배포.

- Vercel에 배포할 수 있게 설정하고, 필요한 환경변수(NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY) 목록과 배포 순서를 README에 정리해줘.

- 프로덕션 빌드(npm run build) 통과 확인.

- 테스트 토큰 링크로 전체 흐름(진입→스크리닝→Day1→Day2→제출→완료)을 한 번 끝까지 통과시켜줘.

확인: build 통과 / 실제 링크로 끝까지 1회 완주 / 모바일에서 동작.


자주 쓸 보조 프롬프트
막힐 때: 방금 단계에서 바뀐 파일과 이유를 요약하고, 모바일 390px 스크린샷을 보여줘.
수정: 다이어리 화면에서 [바꿀 점]만 고치고 다른 건 건드리지 마.
디자인 통일: 프로토타입 HTML의 색/간격/폰트와 어긋난 곳을 찾아 맞춰줘.
에러: 이 에러 로그를 읽고 원인과 최소 수정으로 고쳐줘: [붙여넣기]
순서 요약
①스캐폴드 → ②DB → ③진입/분기 → ④스크리닝 → ⑤다이어리(핵심) → ⑥완료흐름 → ⑦관리자/내보내기 → ⑧품질 → ⑨모바일QA(캡처) → ⑩배포

